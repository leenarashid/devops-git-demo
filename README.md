#Devops NGINX Demo

# This project helps you install NGINX on Linux and then displays a demo page



sudo apt-get update && sudo aapt install nginx -y


sudo systemctl start nginx
sudo systemctl enable nginx


The webpage is kept at /var/www/nginx
